-- Roles already live in users.role (TEXT). Allowed values:
--   client     — default on signup
--   moderator  — can manage bookings and view users
--   admin      — full access, including promoting/demoting staff
-- No schema change required; this file documents the contract.
SELECT 1;
